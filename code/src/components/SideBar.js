import React from 'react';
import CompleteTaskList from './CompleteTaskList';

const SideBar = () => {
  return (
    <section is>
      <CompleteTaskList />
    </section>
  );
};

export default SideBar;

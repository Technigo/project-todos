import React, { Component } from 'react'
import { useSelector } from 'react-redux'
// import { Todo } from './Todo'

//maps lists of todo from useSelector

export const Todos = () => {
  return (
    <div>
    </div>
  )
}

const mapStateToProps = (state) => ({

})

const mapDispatchToProps = {

}


// import React from "react"
// import { useSelector } from "react-redux"

// const NumberOfCompleted = () => {
//     const allTodos = useSelector((store) => store.todos.items.length);

//     const displayLength = () => {
//       return allTodos.filter((item) => item.isComplete).length;
//     };

//       return <h4>{displayLength()}/{allTodos.length} completed</h4>
// }

// export default NumberOfCompleted
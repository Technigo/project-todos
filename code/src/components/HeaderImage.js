import React from 'react';

// import { CategoryImage } from '../styling/GlobalStyling';

export const HeaderImage = ({ source, category }) => {
  return (
    <div>
      <p>{category}</p>
    </div>
  );
  // <CategoryImage src={source} />;
};

import React from "react";

import light from "./assets/light.png";

const NoTodos = () => {
  return (
    <section>
      <img src={light} />
      <p>Every mastermind needs a todo, let's add your tasks"</p>
    </section>
  );
};

export default NoTodos;

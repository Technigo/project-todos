/* eslint-disable linebreak-style */
import { createSlice } from '@reduxjs/toolkit'

const initialState = {
  login: []

  // more items
}

export const login = createSlice({
  name: 'login',
  initialState,
  reducers: {

    // some actions with functions
  }

})
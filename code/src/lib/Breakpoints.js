const size = {
    tablet: "768px",
    computer: "1198px",
   }
   const device = {
    tablet: `(min-width: ${size.tablet})`,
    computer: `(min-width: ${size.computer})`
   }
   export default {size, device}